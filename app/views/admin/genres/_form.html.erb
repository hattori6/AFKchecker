<%= form_with model:genre, url: url do |f| %>
	<%= render 'layouts/error', resource: genre %>
	<%= f.label :name, "タイトル名:" %>
	<%= f.text_field :name %>
	<%= f.submit submit, class: "btn btn-primary ml2" %>
<% end %>